#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s = "Hello World";

    string s2 = "Hi";

    // s += s2;

    // s.append(s2);

    // s.push_back('a');

    // s.pop_back();

    // s = "Hi Universe";

    // s.assign(s2);

    // s.erase(5, 2); // here 5 is index, and 2 is how many character

    // s.replace(6, 5, "Bangladesh");

    s.insert(5, " Tamim");

    cout << s << endl;
    
    return 0;
}